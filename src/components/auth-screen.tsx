import { Link } from "@tanstack/react-router";
import { ArrowLeft } from "lucide-react";
import { Mark } from "./product-shell";

export function AuthScreen({ mode }: { mode: "login" | "register" | "forgot" }) {
  const register = mode === "register";
  const forgot = mode === "forgot";
  const title = forgot ? "Reset your password" : register ? "Create your account" : "Welcome back";
  return <main className="grid min-h-screen bg-background lg:grid-cols-[1fr_1.05fr]"><section className="flex min-h-screen flex-col px-6 py-6 sm:px-10"><div className="flex items-center justify-between"><Link to="/"><Mark /></Link><span className="text-sm text-muted-foreground">{register ? "Already a member?" : "New to Workspaces?"} <Link to={register ? "/login" : "/register"} className="font-semibold text-primary">{register ? "Sign in" : "Create account"}</Link></span></div><div className="mx-auto flex w-full max-w-md flex-1 flex-col justify-center py-12">{forgot && <Link to="/login" className="mb-8 flex items-center gap-2 text-sm font-medium text-muted-foreground"><ArrowLeft className="h-4 w-4" /> Back to sign in</Link>}<p className="text-sm font-semibold text-primary">Your digital office</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-secondary sm:text-4xl">{title}</h1><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{forgot ? "Enter your email and we'll send you a secure reset link." : "A calm place for ideas, people, and visible execution."}</p>{!forgot && <><button className="mt-8 flex h-11 w-full items-center justify-center gap-3 rounded-xl border border-border bg-card text-sm font-semibold text-secondary shadow-sm transition-colors hover:bg-surface"><span className="text-base font-bold text-info">G</span> Continue with Google</button><div className="my-6 flex items-center gap-4"><span className="h-px flex-1 bg-border" /><span className="text-xs text-muted-foreground">or continue with email</span><span className="h-px flex-1 bg-border" /></div></>}<form className="space-y-4">{register && <Field label="Full name" placeholder="Your name" />}<Field label="Email address" placeholder="you@company.com" type="email" />{!forgot && <Field label="Password" placeholder="At least 8 characters" type="password" />} {!forgot && !register && <div className="flex justify-end"><Link to="/forgot-password" className="text-xs font-semibold text-primary">Forgot password?</Link></div>}<Link to={forgot ? "/login" : register ? "/setup" : "/home"} className="flex h-11 w-full items-center justify-center rounded-xl bg-primary text-sm font-semibold text-primary-foreground shadow-sm transition-colors hover:bg-primary/90">{forgot ? "Send reset link" : register ? "Create account" : "Sign in"}</Link></form>{register && <p className="mt-5 text-center text-xs leading-relaxed text-muted-foreground">By continuing, you agree to the Terms and Privacy Policy.</p>}</div></section><aside className="relative hidden overflow-hidden bg-secondary p-14 text-secondary-foreground lg:flex lg:flex-col lg:justify-between"><div className="absolute inset-0 opacity-30 [background-image:radial-gradient(circle_at_20%_10%,var(--primary),transparent_32%)]" /><p className="relative text-xs font-semibold uppercase tracking-[0.2em] text-secondary-foreground/60">Workspaces · Africa</p><div className="relative max-w-lg"><blockquote className="text-4xl font-medium leading-tight tracking-[-0.04em]">“Execution becomes lighter when everyone can see where the work is going.”</blockquote><p className="mt-8 text-sm text-secondary-foreground/60">Built for ambitious teams everywhere.</p></div><div className="relative grid grid-cols-3 gap-3">{["Ideas", "People", "Outcomes"].map((x, i) => <div key={x} className="rounded-2xl border border-white/10 bg-white/5 p-4"><p className="text-xs text-white/50">0{i + 1}</p><p className="mt-8 font-semibold">{x}</p></div>)}</div></aside></main>;
}

function Field({ label, placeholder, type = "text" }: { label: string; placeholder: string; type?: string }) { return <label className="block"><span className="mb-2 block text-xs font-semibold text-secondary">{label}</span><input type={type} placeholder={placeholder} className="h-11 w-full rounded-xl border border-border bg-card px-3.5 text-sm outline-none transition-shadow placeholder:text-muted-foreground/60 focus:border-primary focus:ring-4 focus:ring-primary/10" /></label>; }