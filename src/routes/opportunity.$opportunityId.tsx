import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowLeft, Check, MapPin } from "lucide-react";
import { ProductShell } from "../components/product-shell";
import { opportunities } from "../data/product";
export const Route = createFileRoute("/opportunity/$opportunityId")({ component: Detail });
function Detail(){ const {opportunityId}=Route.useParams(); const item=opportunities.find(x=>x.id===opportunityId)??opportunities[0]; return <ProductShell title="Opportunity" eyebrow="Marketplace"><Link to="/marketplace" className="mb-8 flex items-center gap-2 text-sm font-medium text-muted-foreground"><ArrowLeft className="h-4 w-4"/> Back to marketplace</Link><div className="grid gap-10 lg:grid-cols-[minmax(0,1fr)_320px]"><article><span className="rounded-full bg-primary-soft px-3 py-1 text-xs font-semibold text-primary">{item.type}</span><h2 className="mt-6 text-4xl font-semibold leading-tight text-secondary sm:text-5xl">{item.title}</h2><p className="mt-3 text-lg font-medium text-secondary">{item.org}</p><p className="mt-3 flex items-center gap-2 text-sm text-muted-foreground"><MapPin className="h-4 w-4"/>{item.location}</p><div className="mt-10 border-t border-border pt-8"><h3 className="text-lg font-semibold text-secondary">About the opportunity</h3><p className="mt-4 max-w-2xl text-base leading-7 text-muted-foreground">{item.description} You’ll work in a small, thoughtful team with clear outcomes, open communication, and room to shape how the work gets done.</p><h3 className="mt-10 text-lg font-semibold text-secondary">What we're looking for</h3><div className="mt-4 space-y-3">{["A strong point of view and collaborative mindset","Experience delivering real work with small teams","Clear communication across distributed environments"].map(x=><p key={x} className="flex gap-3 text-sm text-muted-foreground"><Check className="h-5 w-5 shrink-0 text-primary"/>{x}</p>)}</div></div></article><aside><div className="sticky top-24 rounded-2xl border border-border bg-card p-5 shadow-sm"><p className="text-sm font-semibold text-secondary">Ready to contribute?</p><p className="mt-2 text-sm leading-relaxed text-muted-foreground">Introduce yourself to the team. Your profile is shared with this application.</p><button className="mt-6 h-11 w-full rounded-xl bg-primary text-sm font-semibold text-primary-foreground">Apply to join</button><p className="mt-4 text-center text-xs text-muted-foreground">One clear application. No long forms.</p></div></aside></div></ProductShell> }