import { createFileRoute } from "@tanstack/react-router"; import { ProductShell, PrimaryAction } from "../components/product-shell";
export const Route=createFileRoute("/workspace-opportunities")({component:()=> <ProductShell title="Opportunities" action={<PrimaryAction>Publish opportunity</PrimaryAction>}><div className="rounded-2xl border border-dashed border-border bg-surface p-12 text-center"><h2 className="text-xl font-semibold text-secondary">Invite the right people into the work.</h2><p className="mx-auto mt-2 max-w-md text-sm text-muted-foreground">Publish roles and collaborations connected directly to this workspace.</p></div></ProductShell>});