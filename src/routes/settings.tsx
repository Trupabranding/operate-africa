import { createFileRoute } from "@tanstack/react-router"; import { ProductShell } from "../components/product-shell";
export const Route=createFileRoute("/settings")({component:()=> <ProductShell title="Settings"><div className="max-w-2xl space-y-4">{["Account","Notifications","Workspace preferences"].map(x=><section key={x} className="rounded-2xl border border-border bg-card p-5"><h2 className="text-base font-semibold text-secondary">{x}</h2><p className="mt-2 text-sm text-muted-foreground">Manage your {x.toLowerCase()} with simple, focused controls.</p></section>)}</div></ProductShell>});